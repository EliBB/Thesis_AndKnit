package com.example.knit.interfaces;

import com.example.knit.classes.Patterns;
/** Provides a callback method for the pattern items in the Storage screen. */
public interface PatternsListener {
    void onItemClick (Patterns patterns);
}
